# 公网部署配置（复制为 .env.public 并修改）
# 部署: docker compose -f docker-compose.public.yml --env-file .env.public up -d --build

# 已解析到服务器公网 IP 的域名（仅 MCP 对外）
MCP_DOMAIN=mcp.yourdomain.com

# 生产环境务必更换为随机长字符串（可用: openssl rand -hex 32）
API_TOKEN=CHANGE_ME_api_token
MCP_ACCESS_TOKEN=CHANGE_ME_mcp_token
